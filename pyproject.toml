[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ip-scanner"
version = "2.0.0"
description = "A responsive cross-platform LAN IP scanner"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["psutil>=5.9,<8", "dnspython>=2.4,<3", "getmac>=0.9,<1"]

[project.optional-dependencies]
gui = ["PySide6>=6.5,<7"]
dev = ["pytest>=7", "ruff>=0.5", "pyinstaller>=6,<7"]

[project.scripts]
ip-scanner = "ip_scanner.cli:main"
ip-scanner-gui = "ip_scanner.gui:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
ip_scanner = ["assets/*.svg"]

[tool.ruff]
line-length = 100
target-version = "py39"
