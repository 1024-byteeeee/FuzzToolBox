# IP-Scanner 首版技术方案决策

## 结论

采用 **Python 3.9+、PySide6、asyncio、SQLite**。扫描核心不依赖 Qt，桌面界面只是调用核心层，因此后续可以独立替换探测器或增加命令行、服务端入口。

## 对原方案的调整

1. 首版使用“Qt 后台线程 + 单 asyncio 事件循环 + 有界工作池”，暂不叠加多进程。主机发现属于网络 I/O 密集任务，多进程会增加序列化、队列和停止控制成本；只有性能测试证明 CPU 成为瓶颈时再引入多进程。
2. 地址范围用整数边界和生成器惰性遍历。即使输入 `/8`，也不会预先创建 1600 万个字符串或协程。
3. 并发任务数和待处理队列均有上限，内存消耗主要由 `concurrency` 和存活结果数决定，而不是由目标总量决定。
4. 默认只保留存活结果。扫描总数和无响应数使用计数器统计，避免大网段结果占满内存。
5. 首版的 TCP 探测是全连接扫描；TCP 端口拒绝并不能证明主机离线，因此它的语义是“指定端口中至少一个可连接”。需要更完整的主机发现时使用 Ping，后续再增加同二层 ARP 插件。
6. ICMP 首版调用系统 `ping`，避免 raw socket 权限和第三方库差异；这条路径适合中小网段。大网段高性能 ICMP/ARP 需要平台原生后端，不能仅靠提高 asyncio 并发实现。
7. Windows 7 不与 PySide6 同时承诺。当前版本支持 Qt 6 所支持的现代系统；若 Windows 7 是硬性需求，应单独维护 PySide2/Qt 5.15 构建，并冻结 Python 与依赖版本。

## 首版范围

- 已实现：CIDR/范围解析、TCP/Ping、流式进度、停止、CSV/JSON 导出、SQLite 历史存储基础层、桌面 GUI、CLI。
- 后续：ARP 探测、GUI 历史页、扫描结果对比、配置持久化、平台安装包、MAC 厂商库、速率限制。

## 安全与产品约束

- 扫描范围不设置地址数量上限；目标采用惰性迭代和有界队列，避免大范围扫描一次性占用内存。
- 产品界面和文档应提示用户只扫描自有或已授权网络。
- 默认并发数为 256，不以“理论最大并发”作为默认值，避免局域网拥塞和误触 IDS。
